name: Followup Tools Check
on:
  push:
    branches: [main, feature/repair-budget-gaps-20260921]
    paths: [repair-quote-budget.html, decision-followup-v1.js, decision-followup-v1.css, sitemap-growth.xml, tests/followup-tools.test.cjs, .github/workflows/followup-tools-check.yml]
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  followup-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Validate arithmetic, unknown states, imports and private calendar export
        run: node tests/followup-tools.test.cjs
